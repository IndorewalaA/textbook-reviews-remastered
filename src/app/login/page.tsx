import '../globals.css';
import '../components/NavBar';
import NavBar from '../components/NavBar';

export default function Page() {
    return(
        <main>
            <NavBar/>
        </main>
    );
}